<thlogic>
    <attr sel="#head"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#footer"
          th:replace="~{fragments/footer :: footerFragment}"/>
    <attr sel="#homeWelcome"
          th:text="#{home.welcome}"/>
    <attr sel="#playTheGame"
          th:href="play" th:text="#{home.play}"/>
</thlogic>